10 REM GRAPHICS EXPERIMENTING
20 REM X0 = 100
30 REM Y0 = 100
40 CLS
50 SCREEN 2
60 X = 2!
70 Y = 2!
80 FOR I% = 1 TO 5
100 X0 = 300! + 10! * X
110 Y0 = 100! - 10! * Y
120 GOSUB 7000
130 GOSUB 8000
140 NEXT I%

160 END

7000 REM THE SUBROUTINE TO SET ONE PIXEL ON THE SCREEN
7010 REM THIS IS CUMBERSOME....... DRAW A ONE PIXEL LINE
7020 LINE (X0, Y0)-(X0, Y0), 1, B
7030 RETURN
8000 REM COMPLEX ITERATION SUBROUTINE
8010 LET TX = (2! * X * Y) / SQR((X ^ 2 + Y ^ 2))
8020 LET TY = (X ^ 2 - Y ^ 2) / SQR((X ^ 2 + Y ^ 2))
8030 LET X = TX
8040 LET Y = TY
8050 RETURN
9999 END

